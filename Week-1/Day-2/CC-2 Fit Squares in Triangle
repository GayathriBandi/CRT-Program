#include <stdio.h>

int main() {
    int T;
    scanf("%d", &T);

    while (T--) {
        int B;
        scanf("%d", &B);

        B = B / 2;
        int result = (B * (B - 1)) / 2;

        printf("%d\n", result);
    }

    return 0;
}
